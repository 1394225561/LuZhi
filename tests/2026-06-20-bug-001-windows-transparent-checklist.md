# BUG-001 Windows 平台透明窗口修复 — 自测清单

**日期**：2026-06-20
**修复文件**：`src-tauri/src/lib.rs`（setup 钩子）
**关联**：BUG-001（800×600 白色边框）

---

## 1. 编译验证

- [ ] `cargo check` 在 `src-tauri/` 目录下通过（无 error，仅有既有 warning）
- [ ] `cargo build` 在 `src-tauri/` 目录下成功编译

## 2. 功能验证（手动）

### 2.1 启动验证

- [ ] `npm run tauri:dev:ffmpeg` 启动应用
- [ ] 窗口**无** 800×600 白色边框
- [ ] 录制面板正常显示（深色浮动小组件外观）
- [ ] 面板周围区域为透明（可看到桌面或下方窗口）

### 2.2 录制流程验证

- [ ] 点击"开始录制"正常进入录制状态
- [ ] 录制中状态栏正常显示（无白色边框）
- [ ] 点击"停止录制"正常返回预览状态
- [ ] 预览视图正常显示（深色背景）

### 2.3 其他状态视图验证

- [ ] 处理中视图（ProcessingView）正常显示
- [ ] 错误视图（ErrorView）正常显示
- [ ] 所有状态下窗口均可拖动（BUG-002 不回退）

## 3. 回归验证

- [ ] macOS 平台构建不受影响（`#[cfg(windows)]` 隔离）
- [ ] 既有单元测试通过：`cargo test` 在 `src-tauri/` 目录下通过
- [ ] 前端测试通过：`npm run test`

## 4. 边界条件

- [ ] 窗口缩放后无白色边框残留
- [ ] 窗口最小化后恢复无白色闪现
- [ ] 多次启动/关闭无累积副作用

---

## 验证结论

| 项目 | 结果 | 备注 |
| ---- | ---- | ---- |
| 编译 | ⬜ | |
| 启动无白框 | ⬜ | |
| 录制流程 | ⬜ | |
| 回归 | ⬜ | |
