# BUG-0015 & BUG-0016 修复自测清单

## BUG-0015: 历史录制列表为空

### 核心验证
- [ ] 录制结束 → 返回空闲界面 → 侧边栏显示历史录制条目
- [ ] 录制结束时 cursor_metadata_path 为 None → 仍能注册到历史库
- [ ] 录制结束时 effect_timeline_path 为 None → 仍能注册到历史库（仅 video_path 必需）
- [ ] 录制结束时 cut_timeline_path 为 None → 条目正常创建，裁剪路径为 null
- [ ] 重启应用 → 历史录制列表正常加载
- [ ] 启动修复不删除缺少裁剪时间线的条目

### 边界条件
- [ ] 光标追踪未启动时录制 → 条目仍注册（cursor_metadata_path 为 None）
- [ ] 多次录制后 → 列表显示所有条目
- [ ] 删除条目 → 可选文件不存在时不报错

## BUG-0016: 导入失败

### 核心验证
- [ ] 导入录制视频（配套文件时间戳与视频不同）→ 成功
- [ ] 导入时正确找到 cursor-metadata-*-{seq}.json 文件
- [ ] 导入时正确找到 cursor-effects-*-{seq}.json 文件
- [ ] trim-metadata 和 cut-timeline 文件不存在时 → 导入仍成功
- [ ] 导入后重新进入美化流程 → 正常加载上下文

### 边界条件
- [ ] 同一目录有多个同序列号文件 → 选择最新的
- [ ] 缺少 cursor-metadata 文件 → 报错"缺少配套文件"
- [ ] 缺少 cursor-effects 文件 → 报错"缺少配套文件"
- [ ] 元数据文件损坏 → 报错"元数据文件损坏"
- [ ] 重复导入 → 报错"该录制已在历史记录中"

## 光标效果预览叠加层

### 核心验证
- [ ] 录制后进入预览 → 光标位置在视频上正确显示
- [ ] 播放视频时 → 光标位置随时间同步移动
- [ ] 点击事件 → 显示蓝色放大环动画
- [ ] 光标形状：箭头/手型/I-beam 正确渲染
- [ ] 调整光标放大倍数 → 叠加层更新

### 边界条件
- [ ] 视频未加载时 → 叠加层不渲染
- [ ] effectTimeline 为空时 → 叠加层不渲染
- [ ] renderCursorOverlay 为 false 时 → 叠加层不渲染
- [ ] 窗口缩放时 → 叠加层正确响应

## 回归验证
- [ ] 319 个 Rust 单元测试全部通过
- [ ] 63 个前端测试全部通过
- [ ] TypeScript 编译无错误
