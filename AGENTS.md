# 项目约定

针对本仓库的特定项目说明，用于规范 code agent 的工作方式。

---

## 团队约定

- **核心基调**：稳健、兼容、可维护。
- 每轮开发计划，都需要拆分成多个 Phase 进行工作。
- **关键文件**如下：
  - "HANDOFF.md"：
    1. 该文件是**开发任务进度**、**开发细节记录**、**多轮对话之间交接**的文件
    2. **重要**：每次规划任务、正式编写代码前，**务必**读取该文件内容，了解项目进度和项目细节
    3. **重要**：每个 phase 都需要生成一份完整、详细的自测清单，保存到 "./tests" 目录
  - "BUG.md"
    1. 该文件用于跟踪记录 bug 修复情况
    2. **重要**：代码审查时，在原有的标准基础上需要多加一条：验证代码是否遵循了 "BUG.md" 文件中所有的 **预防规则**！
  - "reference/ui/ui_spec.md"
    1. **设计规范**，严格遵循该文件中提及的规范、索引的**移植计划与规范**文件内容。

---

## 协作原则

- **重要**：plan 阶段需要持续向我提问，直到你有 95% 的信心理解我的需求。
- **协作边界**：
  - **AI 负责**：架构决策、代码骨架生成、重复性代码编写、工具函数编写、兼容性代码检索、单测生成。
    - **AI 可以做** ：脚手架搭建、UI 还原、正则/算法草稿、单元测试生成、重构建议、文档补全。
    - **AI 绝对不能做** ：
      - 直接修改 `Cargo.toml` 核心依赖版本。
        - 跨平台底层 API（ScreenCaptureKit/DXGI/WASAPI）的底层安全调用，AI 生成后必须由人工逐行审查内存安全与并发安全。
        - 设计产品路线图外的功能。
  - **人类负责**：业务逻辑确认、UI 细节确认。
- **AI 红线**：AI 不得自动执行 git push、npm publish 或修改生产环境配置文件。
- **Code Review 口径**：
  - AI 在执行代码 Review 时，必须重点检查 AI 生成的以下区域：
    > **性能 Review** ：针对 AI 生成的视频帧处理、光标渲染代码，需**重点**审查是否有内存泄漏、不必要的拷贝、阻塞主线程的风险。
  - **零信任验证** ：
    > AI 生成的代码视作“未经验证的第三方代码”，必须经过同等严格的人工 Review。

---

## 常用命令

项目中常用的命令

---

## 架构背景

- 参考：".codex/rules/0-global.md"

---

## 规则索引

指向 .codex/rules/ 目录下的规则分流文件。**编码前必须仔细阅读**。

### 跨技术栈通用规则

1. ".codex/rules/0-global.md"
   1. 项目结构
   2. 项目级全局规划
   3. ...
2. ".codex/rules/1-coding-style.md"
   1. 代码风格、命名、格式
   2. ...
3. ".codex/rules/2-testing.md"
   1. 测试要求、测试覆盖率、mock 策略
   2. ...
4. ".codex/rules/3-git-commit.md"
   1. 提交信息、分支、PR 口径
   2. ...
5. ".codex/rules/4-security.md"
   1. 权限、注入、秘钥处理
   2. ...
6. ".codex/rules/5-docs.md"
   1. 文档结构
   2. 注释口径
   3. ...

---
